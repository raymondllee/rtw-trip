[phases.setup]
nixPkgs = ["python311", "nodejs_20", "pip"]

[phases.install]
cmds = [
    "npm install",
    "pip install -r requirements.txt"
]

[phases.build]
cmds = [
    "npm run build:config"
]

[start]
cmd = "bash start.sh"
